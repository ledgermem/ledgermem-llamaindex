name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    uses: ledgermem/.github/.github/workflows/python-test.yml@main
    with:
      python-versions: '["3.10", "3.11", "3.12"]'
